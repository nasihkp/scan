
  # SmartScan Mobile Scanner UI

  This is a code bundle for SmartScan Mobile Scanner UI. The original project is available at https://www.figma.com/design/U9a05buwa1exBXeZk0pbSv/SmartScan-Mobile-Scanner-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  